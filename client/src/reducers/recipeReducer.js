import searchConstants from "../constants/search_constants";

const initialState = {
  isSearching: false,
};

export default (state=initialState, action) => {
  switch(action.type){
    case
  }
}
